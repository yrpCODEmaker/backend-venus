# Venus Backend — Dependencias
fastapi
uvicorn[standard]
aiosqlite
pydantic[email]
pydantic-settings
python-jose[cryptography]
bcrypt
python-multipart
Pillow

# ── Seguridad Avanzada (2FA, QR, GeoIP) ──
pyotp
qrcode
geoip2

# ── Generación de Facturas ──
weasyprint        # HTML → PDF (requiere Cairo/GTK en Linux/Docker)
pypdfium2         # Renderizador PDF → PNG puro Python (sin dependencias binarias externas)
jinja2            # Motor de templates para el HTML de la factura

# ── Dev / Tests ──
pytest
pytest-asyncio
httpx
